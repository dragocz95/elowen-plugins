{
  "name": "web",
  "version": "0.2.0",
  "apiVersion": "1",
  "description": "Web research: WebSearch (Tavily) and WebFetch (fetch a page as readable text). Modeled on the Hermes web toolset.",
  "entry": "index.mjs",
  "provides": { "tools": ["WebSearch", "WebFetch"] },
  "configSchema": [
    { "key": "tavilyApiKey", "label": "Tavily API key", "type": "secret", "hint": "app.tavily.com — powers WebSearch. Without it only WebFetch works." },
    { "key": "maxResults", "label": "Search results", "type": "number", "hint": "How many results WebSearch returns (default 5)." }
  ]
}
