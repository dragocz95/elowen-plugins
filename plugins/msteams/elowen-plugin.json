{
  "name": "msteams",
  "version": "0.1.0",
  "apiVersion": "1",
  "description": "Microsoft Teams bot (Azure Bot Framework): message it in a personal chat, group chat or team channel and it answers from Elowen AI. Incoming activities arrive on the daemon webhook /hooks/msteams/messages (validated against Microsoft's JWT); replies, typing indicators and images go out through the Bot Connector REST API. Slash commands (/model, /reasoning, /display, /new, /help) with Adaptive Card pickers, AskUserQuestion as tappable cards, live tool activity, proactive cron/tick pushes and a downloadable Teams app package. Chat tools (admin sessions): conversation & member info, member lists, list conversations and person lookup; plus send a message to any chat, message a person proactively by e-mail or name (the bot opens the 1:1 chat itself), and raw connector access (operator). Each rolePolicy maps an Entra object ID / UPN / conversation ID to allowed Elowen projects + a role prompt.",
  "entry": "index.mjs",
  "provides": {
    "platforms": ["msteams"],
    "httpRoutes": ["messages"],
    "apiRoutes": ["/plugins/msteams/app-package"],
    "tools": [
      "TeamsSend",
      "TeamsMessagePerson",
      "TeamsFindPerson",
      "TeamsChatInfo",
      "TeamsMembers",
      "TeamsMemberInfo",
      "TeamsListConversations",
      "TeamsApi"
    ]
  },
  "icons": { "Teams*": "💼" },
  "showOutput": ["Teams*"],
  "planSafe": ["TeamsFindPerson", "TeamsChatInfo", "TeamsMembers", "TeamsMemberInfo", "TeamsListConversations"],
  "configSchema": [
    { "key": "sec_connection", "label": "Connection", "type": "section", "hint": "Azure Bot credentials. Setup: 1) Entra admin center → App registrations → New registration (single tenant) → copy the Application (client) ID and tenant ID, then Certificates & secrets → New client secret. 2) Azure portal → Create an Azure Bot bound to that app ID; set the messaging endpoint to https://<your-domain>/hooks/msteams/messages and enable the Microsoft Teams channel. 3) Download the app package (button above) and upload it in the Teams admin center → Manage apps." },
    { "key": "appId", "label": "Microsoft App ID", "type": "string", "required": true, "hint": "The Entra app registration's Application (client) ID — also the bot ID in the Azure Bot resource." },
    { "key": "appPassword", "label": "Client secret", "type": "secret", "required": true, "hint": "A client secret from the app registration (Certificates & secrets). Used to call the Bot Connector API." },
    { "key": "tenantId", "label": "Tenant ID", "type": "string", "required": true, "hint": "Your Entra Directory (tenant) ID — the bot authenticates against this tenant (single-tenant app registration)." },
    { "key": "notifyConversationId", "label": "Notification conversation", "type": "string", "hint": "Where proactive messages (cron/tick results) are posted — a Teams conversation ID the bot has already seen, or a person (e-mail / Entra object ID / display name) whose 1:1 chat the bot opens itself. Empty = no proactive pushes." },
    { "key": "sec_proactive", "label": "Reaching people first", "type": "section", "hint": "The bot can start a chat with someone instead of only answering. It remembers everyone it meets (name, Entra object ID, UPN and their 1:1 chat) from incoming messages and conversation rosters — no extra Microsoft permission — and TeamsMessagePerson writes to them by e-mail or name. The settings below are only needed for people the bot has never seen." },
    { "key": "graphLookup", "label": "Microsoft Graph lookup", "type": "boolean", "default": false, "hint": "Off: the bot can only write to people it has already met in Teams. On: an unknown e-mail is resolved against the tenant directory and the Teams app is installed for that person so a chat can be opened. Requires the APPLICATION permissions User.ReadBasic.All and TeamsAppInstallation.ReadWriteSelfForUser.All with tenant admin consent." },
    { "key": "graphCatalogAppId", "label": "Teams catalog app ID", "type": "string", "visibleWhen": { "key": "graphLookup", "equals": true }, "hint": "The app's ID in the organisation's Teams catalog (Teams admin center → Manage apps → the uploaded Elowen app). Needed to install the app for a user who does not have it yet; leave empty if the app is deployed by Teams app policy." },
    { "key": "sec_replies", "label": "Replies & behavior", "type": "section", "hint": "When and how the bot answers." },
    { "key": "respondWithoutMention", "label": "Respond without mention", "type": "boolean", "default": true, "hint": "On: in group chats the bot answers every message from a mapped sender. Off: it only answers when @mentioned. Team channel posts always require an @mention — Teams only delivers those to the bot." },
    { "key": "toolActivity", "label": "Tool activity", "type": "enum", "default": "status", "options": [ { "value": "off", "label": "Off" }, { "value": "status", "label": "Live status" }, { "value": "live", "label": "Live output" } ], "hint": "What the progress message shows while the agent works: nothing, tool status lines, or status plus live output tails." },
    { "key": "answerMode", "label": "Answer delivery", "type": "enum", "default": "final", "options": [ { "value": "final", "label": "Final answer only" }, { "value": "live", "label": "Stream answer live" } ], "hint": "Final posts one reply when the turn ends; live streams the answer into a message as it is written." },
    { "key": "toolOutput", "label": "Tool output detail", "type": "enum", "default": "summary", "options": [ { "value": "hidden", "label": "Status only" }, { "value": "summary", "label": "Short summary" }, { "value": "tail", "label": "Output tail" } ], "hint": "How much of each tool's result the progress message keeps once the tool finishes." },
    { "key": "toolMessageMode", "label": "Tool message layout", "type": "enum", "default": "single", "options": [ { "value": "single", "label": "One live message" }, { "value": "per_tool", "label": "One message per tool" } ], "hint": "Single keeps one edited progress message; per-tool posts a message per tool call." },
    { "key": "runtimeFooter", "label": "Runtime footer", "type": "boolean", "default": true, "hint": "Append a small \"model · context %\" line under the final reply." },
    { "key": "showReasoning", "label": "Show reasoning", "type": "boolean", "default": false, "hint": "Stream the model's reasoning into the progress message (extended-thinking models). Off by default — it's usually noise." },
    { "key": "language", "label": "Service language", "type": "enum", "default": "en", "options": [ { "value": "en", "label": "English" }, { "value": "cs", "label": "Čeština" }, { "value": "sk", "label": "Slovenčina" } ], "hint": "Language of the bot's own service messages (errors, command replies). The agent itself answers in the user's language." },
    { "key": "sec_conversation", "label": "Conversation", "type": "section", "hint": "Context loading and image routing." },
    { "key": "historyLimit", "label": "Conversation history backfill", "type": "number", "min": 0, "max": 100, "step": 1, "placeholder": "0", "default": 0, "hint": "How many recent messages a BRAND-NEW conversation loads as context (0 = off, max 100). Teams gives a bot no way to read past messages, so the bot remembers and stores what it saw while present; ongoing conversations never reload it." },
    { "key": "visionModel", "label": "Vision model", "type": "model", "hint": "Model used for turns that carry image attachments (must support vision). Empty = the chat's normal model." },
    { "key": "sec_media", "label": "Media limits", "type": "section", "advanced": true, "hint": "Caps on inbound/outbound images and how long a pending question stays open." },
    { "key": "maxImageBytes", "label": "Max inbound image size", "type": "number", "min": 1048576, "max": 20971520, "step": 1048576, "placeholder": "5242880", "default": 5242880, "hint": "Largest image attachment (bytes) the bot downloads for vision." },
    { "key": "maxImages", "label": "Max images per message", "type": "number", "min": 1, "max": 10, "step": 1, "placeholder": "4", "default": 4, "hint": "How many image attachments in a single incoming message the bot sends to the vision model." },
    { "key": "maxUploadImages", "label": "Max images per reply", "type": "number", "min": 1, "max": 10, "step": 1, "placeholder": "4", "default": 4, "hint": "How many generated images the bot attaches to a single outgoing reply." },
    { "key": "askTimeoutMs", "label": "Question timeout (ms)", "type": "number", "min": 30000, "max": 1800000, "step": 30000, "placeholder": "360000", "default": 360000, "hint": "How long a pending AskUserQuestion card stays answerable." },
    { "key": "sec_roles", "label": "Role policies", "type": "section", "hint": "Map Teams senders to allowed projects, a role prompt and the tools the bot may use." },
    { "key": "rolePolicies", "label": "Role policies", "type": "rolePolicies", "hint": "First matching policy wins; senders with no mapped policy are ignored. The ID is the sender's Entra object ID (a GUID), their UPN/email, or a whole conversation ID (grants everyone in that chat)." }
  ]
}
