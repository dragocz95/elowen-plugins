{
  "name": "image-gen",
  "version": "0.2.4",
  "apiVersion": "1",
  "requiresCore": "0.28.41",
  "description": "Image generation through a configured API-key provider or the connected ChatGPT account. Generated images show directly in the chat.",
  "entry": "index.mjs",
  "requires": {
    "config": [
      "provider"
    ]
  },
  "provides": {
    "tools": [
      "GenerateImage"
    ]
  },
  "configSchema": [
    {
      "key": "provider",
      "label": "Provider",
      "type": "provider",
      "providerType": [
        "openai",
        "oauth-openai-codex"
      ],
      "required": true,
      "hint": "Which configured provider renders the images: an OpenAI-compatible endpoint (its key and base URL are reused) or the connected ChatGPT account. Add providers in Settings → Brain."
    },
    {
      "key": "model",
      "label": "Model",
      "type": "model",
      "modelKind": "image",
      "placeholder": "gpt-image-2.5-sunburst",
      "hint": "Image model of the chosen provider. The picker offers the models enabled for that provider in Settings → Models; enable one there first. Empty uses the provider's default."
    },
    {
      "key": "size",
      "label": "Size",
      "type": "enum",
      "default": "1024x1024",
      "options": [
        {
          "value": "1024x1024",
          "label": "1024×1024 (square)"
        },
        {
          "value": "1536x1024",
          "label": "1536×1024 (landscape)"
        },
        {
          "value": "1024x1536",
          "label": "1024×1536 (portrait)"
        }
      ],
      "hint": "Output resolution and aspect ratio."
    }
  ]
}
