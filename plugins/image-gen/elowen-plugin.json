{
  "name": "image-gen",
  "version": "0.2.1",
  "apiVersion": "1",
  "description": "Image generation via the OpenAI Images API (gpt-image-1 by default). Generated images show directly in the chat.",
  "entry": "index.mjs",
  "requires": {
    "config": [
      "provider"
    ]
  },
  "provides": {
    "tools": [
      "GenerateImage"
    ]
  },
  "configSchema": [
    {
      "key": "provider",
      "label": "Provider",
      "type": "provider",
      "providerType": "openai",
      "required": true,
      "hint": "Which configured OpenAI-compatible provider to use (reuses its key + base URL). Add providers in Settings → Brain."
    },
    {
      "key": "model",
      "label": "Model",
      "type": "model",
      "hint": "Pick from your configured models. Default: gpt-image-1."
    },
    {
      "key": "size",
      "label": "Size",
      "type": "string",
      "hint": "1024x1024 (default), 1536x1024 or 1024x1536."
    }
  ]
}