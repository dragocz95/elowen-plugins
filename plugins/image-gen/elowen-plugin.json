{
  "name": "image-gen",
  "version": "0.2.1",
  "apiVersion": "1",
  "description": "Image generation via the OpenAI Images API (gpt-image-1 by default). Generated images show directly in the chat.",
  "entry": "index.mjs",
  "requires": {
    "config": [
      "provider"
    ]
  },
  "provides": {
    "tools": [
      "GenerateImage"
    ]
  },
  "configSchema": [
    {
      "key": "provider",
      "label": "Provider",
      "type": "provider",
      "providerType": "openai",
      "required": true,
      "hint": "Which configured OpenAI-compatible provider to use (reuses its key + base URL). Add providers in Settings → Brain."
    },
    {
      "key": "model",
      "label": "Model",
      "type": "model",
      "default": "gpt-image-1",
      "hint": "Pick from your configured models. Default: gpt-image-1."
    },
    {
      "key": "size",
      "label": "Size",
      "type": "enum",
      "default": "1024x1024",
      "options": [
        { "value": "1024x1024", "label": "1024×1024 (square)" },
        { "value": "1536x1024", "label": "1536×1024 (landscape)" },
        { "value": "1024x1536", "label": "1024×1536 (portrait)" }
      ],
      "hint": "Output resolution and aspect ratio."
    }
  ]
}