{
  "name": "sites",
  "version": "0.4.0",
  "apiVersion": "1",
  "requiresCore": "0.28.19",
  "description": "Publish a web page the agent built - a dashboard, a report, a landing page - at a real address on this instance, and decide who may open it: only you, the Project's people, any signed-in account, named guests, or everyone. The agent builds with its own toolchain and hands over the finished output; every publish is a separate release you can roll back to.",
  "entry": "dist/index.js",
  "provides": {
    "tools": [
      "SiteCreate",
      "SitePublish",
      "SiteGet",
      "SiteList",
      "SiteUpdate",
      "SiteRollback",
      "SiteLogs",
      "SiteShare",
      "SiteUnshare",
      "SiteDelete"
    ],
    "httpRoutes": [
      "s"
    ],
    "apiRoutes": [
      "sites",
      "site",
      "ticket",
      "directory"
    ]
  },
  "planSafe": [
    "SiteList",
    "SiteGet",
    "SiteLogs"
  ],
  "icons": {
    "Site*": "🌐"
  },
  "capabilities": {
    "reads": [
      "db",
      "stores",
      "controls"
    ],
    "network": true
  },
  "configSchema": [
    {
      "key": "publishing",
      "label": "Publishing",
      "type": "section"
    },
    {
      "key": "defaultVisibility",
      "label": "Default visibility",
      "type": "enum",
      "default": "private",
      "options": [
        {
          "value": "private",
          "label": "Only the owner"
        },
        {
          "value": "project",
          "label": "The Project's people"
        },
        {
          "value": "authenticated",
          "label": "Any signed-in account"
        }
      ],
      "hint": "What a newly created site is visible to before anyone changes it. A site never becomes public on its own; that is confirmed here in the app."
    },
    {
      "key": "allowPublicSites",
      "label": "Allow public sites",
      "type": "boolean",
      "default": true,
      "hint": "When off, every site requires signing in and the public option disappears from each site's access settings."
    },
    {
      "key": "siteHostOverride",
      "label": "Base hostname for sites",
      "type": "string",
      "default": "",
      "advanced": true,
      "placeholder": "https://sites.example.com",
      "hint": "Serve each published site on its own subdomain of this hostname, so every site is a separate origin: one site cannot read another, and none of them can reach the app's session. Without it, sites are served next to the app and their scripts stay disabled.",
      "help": "Point a wildcard record (*.example.com) at this machine, give it a wildcard certificate, and forward it to the daemon with the request path unchanged. A site is then addressed at <slug>.<this hostname>."
    },
    {
      "key": "publishers",
      "label": "Who may publish",
      "type": "enum",
      "default": "everyone",
      "options": [
        {
          "value": "everyone",
          "label": "Every account"
        },
        {
          "value": "admins",
          "label": "Administrators only"
        }
      ],
      "hint": "Whether an ordinary account's agent may create and publish sites. Opening a site somebody shared is unaffected."
    },
    {
      "key": "runtimes",
      "label": "Site runtimes",
      "type": "section"
    },
    {
      "key": "allowCommandRuntime",
      "label": "Allow site runtimes",
      "type": "boolean",
      "default": false,
      "risk": "high",
      "hint": "Let a site be a running server - Node, Bun, Python, PHP - instead of files on disk. The process is confined to its own namespace and sees only its own release directory, but it runs under the daemon's account, keeps that account's sandbox home and shares the machine's network. Requires a dedicated site hostname.",
      "help": "A published server is code the agent wrote, reachable from the network. Confinement here is a namespace, not a separate machine account: the process cannot see other projects, but it can reach the network as this machine, it holds the owner's sandbox home directory, and it can write inside its own release. There is no CPU or memory limit. Requests reach it buffered, so it cannot stream, use server-sent events or accept websockets, and a request body is capped at 1 MB."
    },
    {
      "key": "startTimeoutSeconds",
      "label": "Start timeout",
      "type": "number",
      "default": 30,
      "min": 5,
      "max": 300,
      "advanced": true,
      "hint": "How long a runtime has to answer after it is started before the publish is treated as failed."
    },
    {
      "key": "requestTimeoutSeconds",
      "label": "Request timeout",
      "type": "number",
      "default": 15,
      "min": 1,
      "max": 120,
      "advanced": true,
      "hint": "How long one exchange with a runtime may take in total before the visitor gets an error."
    },
    {
      "key": "maxResponseMb",
      "label": "Largest runtime response",
      "type": "number",
      "default": 8,
      "min": 1,
      "max": 64,
      "advanced": true,
      "hint": "A runtime answer is buffered whole before it is sent on, so this is a real ceiling."
    },
    {
      "key": "portRangeStart",
      "label": "First loopback port",
      "type": "number",
      "default": 43000,
      "min": 1024,
      "max": 65000,
      "advanced": true,
      "hint": "Only for a runtime that cannot listen on a unix socket. A loopback port is reachable by every process on this machine, which bypasses the site's access rules."
    },
    {
      "key": "portRangeEnd",
      "label": "Last loopback port",
      "type": "number",
      "default": 43099,
      "min": 1024,
      "max": 65535,
      "advanced": true,
      "hint": "End of the range above."
    },
    {
      "key": "limits",
      "label": "Limits",
      "type": "section"
    },
    {
      "key": "maxAssetMb",
      "label": "Largest file",
      "type": "number",
      "default": 8,
      "min": 1,
      "max": 64,
      "hint": "Files larger than this are refused when publishing. A published file is held in memory while it is served, so this is a real ceiling rather than a preference."
    },
    {
      "key": "maxSiteMb",
      "label": "Largest site",
      "type": "number",
      "default": 200,
      "min": 1,
      "max": 4096,
      "hint": "Total size of one published release."
    },
    {
      "key": "maxSitesPerAccount",
      "label": "Sites per account",
      "type": "number",
      "default": 20,
      "min": 1,
      "max": 500,
      "hint": "How many sites one account may keep at once."
    },
    {
      "key": "releasesKept",
      "label": "Releases kept",
      "type": "number",
      "default": 5,
      "min": 1,
      "max": 50,
      "advanced": true,
      "hint": "Older releases beyond this count are removed. The release a site currently serves is never removed, whatever this is set to."
    },
    {
      "key": "access",
      "label": "Access",
      "type": "section"
    },
    {
      "key": "sessionTtlHours",
      "label": "Sign-in validity",
      "type": "number",
      "default": 12,
      "min": 1,
      "max": 720,
      "hint": "How long a visitor stays signed in to a site before the browser is sent back through the sign-in step. Losing access takes effect immediately regardless of this, because permission is checked on every request."
    }
  ],
  "web": {
    "entry": "web/index.js",
    "css": "web/index.css",
    "requiresApiVersion": 7,
    "label": "Sites",
    "navKind": "domain",
    "nav": [
      {
        "label": "Sites",
        "icon": "Globe"
      }
    ],
    "project": [
      {
        "id": "sites",
        "label": "Sites",
        "icon": "Globe"
      }
    ],
    "strings": {
      "title": "Sites",
      "subtitle": "Pages your agent published, and who can open them.",
      "mine": "My sites",
      "shared": "Shared with me",
      "empty": "No sites yet. Ask the agent to build one and publish it.",
      "emptyShared": "Nobody has shared a site with you.",
      "emptySearch": "No site matches this filter.",
      "loadFailed": "The sites could not be loaded.",
      "searchPlaceholder": "Search by name, address or owner",
      "filterVisibility": "Visibility",
      "filterStatus": "Status",
      "filterAllVisibilities": "Any visibility",
      "filterAllStatuses": "Any status",
      "columnSite": "Site",
      "columnOwner": "Owner",
      "columnVisibility": "Visibility",
      "columnStatus": "Status",
      "columnPublished": "Published",
      "metricTotal": "Sites",
      "metricLive": "Live",
      "metricShared": "Shared with me",
      "metricPublic": "Public",
      "detailTitle": "Site detail",
      "openSite": "Open",
      "copyLink": "Copy address",
      "copied": "Address copied",
      "builtBy": "Built by {model}",
      "neverPublished": "Not published yet",
      "statusLive": "Live",
      "statusDraft": "Draft",
      "statusFailed": "Needs attention",
      "visibilityPrivate": "Only me",
      "visibilityProject": "Project",
      "visibilityAuthenticated": "Signed in",
      "visibilityPublic": "Public",
      "runtime": "Runtime",
      "runtimeRunning": "Running",
      "runtimeStopped": "Not running",
      "runtimeCommand": "Start command",
      "runtimeLog": "Recent output",
      "runtimeEmptyLog": "Nothing recorded yet.",
      "restart": "Restart",
      "restarted": "Restarting",
      "address": "Address",
      "lastPublish": "Last publish",
      "releases": "Releases",
      "releaseSummary": "{files} files · {size}",
      "noReleases": "No release has been published yet.",
      "rollback": "Restore this release",
      "rollbackDone": "Release restored",
      "visits": "Visits",
      "whoCanOpen": "Who can open this",
      "guests": "Named guests",
      "guestsHint": "These accounts can open the site whatever the setting above says.",
      "noGuests": "Nobody has been named yet.",
      "manageGuests": "Manage guests",
      "guestsPickerTitle": "Named guests",
      "guestsPickerSubtitle": "Choose the accounts that may open this site regardless of its visibility.",
      "guestsGroup": "Accounts",
      "guestsCount": "{n} selected",
      "removeGuest": "Remove",
      "publicWarning": "Anyone who knows the address will be able to open this site without signing in.",
      "publicConfirm": "Make public",
      "publicDisabled": "Public sites are turned off for this instance.",
      "passiveNotice": "Scripts are disabled at this address. Set a dedicated site hostname in the plugin settings to run them.",
      "sourceNotice": "The source folder lives in the Project, so everyone with access to the Project can read it. This setting governs the published site.",
      "deleteTitle": "Delete this site",
      "deleteHint": "The address stops working and every release is removed. The source folder in the Project is left alone.",
      "delete": "Delete site",
      "deleted": "Site deleted",
      "saved": "Saved",
      "close": "Close",
      "releaseLive": "Live"
    }
  }
}
