// Type declaration for scheduleGrammar.mjs, so the TypeScript browser bundle (web-src/*.ts, checked
// under tsconfig.plugins-web.json, which does not enable allowJs) can import it with real types instead
// of falling back to `any`. Keep this in sync with the exports in scheduleGrammar.mjs by hand — the
// runtime values are RegExp literals, which JSDoc/allowJs inference could not have given us anyway.
export declare const EVERY_PATTERN: RegExp;
export declare const DAILY_PATTERN: RegExp;
export declare const WEEKLY_PATTERN: RegExp;
