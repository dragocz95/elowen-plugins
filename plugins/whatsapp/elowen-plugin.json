{
  "name": "whatsapp",
  "version": "0.2.15",
  "apiVersion": "1",
  "requiresCore": "0.28.11",
  "requiresSharedApi": 3,
  "description": "WhatsApp bot (Baileys): write to it and it answers from Elowen AI. Text commands (/model, /new, /help), a per-chat model picker (numbered text menu you reply to with a number), live streaming replies (edit-in-place with the agent's tool trace), status reactions and proactive cron/tick pushes. Group tools: list & inspect groups, create groups, add/remove members, and send a message to any chat (operator). Each sender (phone number, JID or whole group) maps to allowed Elowen projects + a role prompt. Pair by scanning a QR code or with a pairing code.",
  "entry": "index.mjs",
  "provides": {
    "platforms": [
      "whatsapp"
    ],
    "tools": [
      "WhatsappSend",
      "WhatsappGroupList",
      "WhatsappGroupInfo",
      "WhatsappGroupCreate",
      "WhatsappGroupAdd",
      "WhatsappGroupRemove"
    ],
    "apiRoutes": [
      "/plugins/whatsapp/pairing",
      "/plugins/whatsapp/pair",
      "/plugins/whatsapp/unpair"
    ]
  },
  "icons": {
    "Whatsapp*": "🟢"
  },
  "configSchema": [
    {
      "key": "sec_connection",
      "label": "Connection",
      "type": "section",
      "hint": "How the bot links to WhatsApp and where it pushes proactively. Pair by scanning the QR code from the plugin logs, or with a pairing code."
    },
    {
      "key": "phoneNumber",
      "label": "Pairing phone number",
      "type": "string",
      "hint": "Optional: the bot's WhatsApp number in international format WITHOUT +, spaces or dashes (e.g. 420777123456). When set, the plugin prints an 8-character pairing code to the logs instead of a QR — enter it in WhatsApp → Linked devices → Link with phone number. Leave empty to pair by QR."
    },
    {
      "key": "groupIds",
      "label": "Allowed groups",
      "type": "tokenList",
      "hint": "Group JIDs (…@g.us) where the bot may respond. Empty = every group where a mapped sender writes. Direct chats remain allowed for mapped senders."
    },
    {
      "key": "notifyChat",
      "label": "Notification chat",
      "type": "string",
      "hint": "Where proactive messages (cron/tick results, escalations, restart notices) are posted — a phone number or a JID (…@s.whatsapp.net / …@g.us). Empty = no proactive pushes."
    },
    {
      "key": "sec_replies",
      "label": "Replies & behavior",
      "type": "section",
      "hint": "When and how the bot answers."
    },
    {
      "key": "respondWithoutMention",
      "label": "Respond without mention",
      "type": "boolean",
      "default": true,
      "hint": "On: in groups the bot answers every message from a mapped sender. Off: in groups it only answers when @mentioned or when its message is replied to. Direct chats always get an answer."
    },
    {
      "key": "streaming",
      "label": "Live progress trace",
      "type": "boolean",
      "default": true,
      "hint": "Show the agent's tool trace in a message edited in place while it works. The answer itself always arrives as one message at the end. Off = only the final answer."
    },
    {
      "key": "deleteToolActivityAfterTurn",
      "label": "Replace tool activity with answer",
      "type": "boolean",
      "default": false,
      "hint": "Show one live progress message while the agent works, then replace that same message with the final answer. No message is deleted."
    },
    {
      "key": "reactions",
      "label": "Processing reactions",
      "type": "boolean",
      "default": true,
      "hint": "Shows processing status using reactions on the incoming message."
    },
    {
      "key": "runtimeFooter",
      "label": "Runtime footer",
      "type": "boolean",
      "default": true,
      "hint": "Append a small \"model · context %\" line under the final reply."
    },
    {
      "key": "showReasoning",
      "label": "Show reasoning",
      "type": "boolean",
      "default": false,
      "hint": "Stream the model's reasoning into the progress message (extended-thinking models). Off by default — it's usually noise."
    },
    {
      "key": "language",
      "label": "Service language",
      "type": "enum",
      "default": "en",
      "options": [
        {
          "value": "en",
          "label": "English"
        },
        {
          "value": "cs",
          "label": "Čeština"
        },
        {
          "value": "sk",
          "label": "Slovenčina"
        }
      ],
      "hint": "Language of the bot's own service messages (/new, /model, placeholders)."
    },
    {
      "key": "sec_conversation",
      "label": "Conversation",
      "type": "section",
      "hint": "Image routing."
    },
    {
      "key": "visionModel",
      "label": "Vision model",
      "type": "model",
      "hint": "Model used for turns that carry image attachments (must support vision). Empty = the chat's normal model."
    },
    {
      "key": "sec_media",
      "label": "Media limits",
      "type": "section",
      "advanced": true,
      "hint": "Caps on inbound/outbound images and how long a pending question stays open."
    },
    {
      "key": "maxImageBytes",
      "label": "Max inbound image size",
      "type": "number",
      "min": 1048576,
      "max": 20971520,
      "step": 1048576,
      "placeholder": "5242880",
      "default": 5242880,
      "hint": "Largest image attachment (bytes) the bot downloads for vision; bigger ones are noted instead."
    },
    {
      "key": "maxImages",
      "label": "Max images per message",
      "type": "number",
      "min": 1,
      "max": 10,
      "step": 1,
      "placeholder": "4",
      "default": 4,
      "hint": "How many image attachments in a single incoming message the bot sends to the vision model."
    },
    {
      "key": "maxUploadImages",
      "label": "Max images per reply",
      "type": "number",
      "min": 1,
      "max": 10,
      "step": 1,
      "placeholder": "4",
      "default": 4,
      "hint": "How many generated images the bot attaches to a single outgoing reply."
    },
    {
      "key": "askTimeoutMs",
      "label": "Question timeout (ms)",
      "type": "number",
      "min": 30000,
      "max": 1800000,
      "step": 30000,
      "placeholder": "360000",
      "default": 360000,
      "hint": "How long a pending AskUserQuestion stays open for a numbered reply or free text before it's dropped as stale. The same window applies to the interactive menus."
    },
    {
      "key": "sec_senders",
      "label": "Sender policies",
      "type": "section",
      "hint": "Map WhatsApp senders to allowed projects, a role prompt and the tools the bot may use."
    },
    {
      "key": "senderPolicies",
      "label": "Sender policies",
      "type": "rolePolicies",
      "hint": "First matching sender wins; senders with no mapped policy are ignored. The ID is a phone number (420777123456), a JID (…@s.whatsapp.net) or a whole group JID (…@g.us) — a group ID grants access to everyone in it. A * matches every sender and belongs last, below the named entries it would otherwise swallow. Mark a policy admin to let it switch the model and use the group tools. Projects and tool permissions come from the sender's linked Elowen account."
    }
  ],
  "web": {
    "entry": "web/index.js",
    "css": "web/index.css",
    "requiresApiVersion": 1,
    "settings": [
      {
        "id": "pairing",
        "label": "Pairing",
        "icon": "QrCode"
      }
    ],
    "strings": {
      "pairTitle": "Pairing",
      "pairHint": "Link the bot to a WhatsApp account. Scan the QR in WhatsApp → Linked devices, or use the pairing code.",
      "pairButton": "Pair device",
      "pairModalTitle": "Pair WhatsApp",
      "pairScan": "Open WhatsApp → Linked devices → Link a device, and scan this code:",
      "pairCode": "Or link with your phone number using this code:",
      "pairWaiting": "Waiting for a QR code… make sure the plugin is enabled.",
      "pairRefresh": "New code",
      "pairError": "Could not reach the WhatsApp plugin. Is it enabled?",
      "unpairButton": "Unpair device",
      "unpairConfirm": "Unlink this WhatsApp account? The bot will stop receiving messages until you pair again.",
      "retry": "Retry",
      "close": "Close"
    }
  }
}
