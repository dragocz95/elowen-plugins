{
  "name": "formatters",
  "version": "0.1.0",
  "apiVersion": "1",
  "description": "Automatically format files the assistant writes, using the formatter the project itself is set up for (prettier, biome, pint, ruff, gofmt, rustfmt, ...). A formatter only runs when its binary or config is present in the project.",
  "entry": "index.mjs",
  "configSchema": [
    {
      "key": "enabled",
      "label": "Automatic formatting",
      "type": "boolean",
      "default": true,
      "hint": "Run the project's formatter right after Write/Edit succeeds. Formatting is on by default; switch this off to disable it entirely."
    },
    {
      "key": "disabled",
      "label": "Disabled formatters",
      "type": "multiSelect",
      "options": [
        { "value": "biome", "label": "Biome" },
        { "value": "prettier", "label": "Prettier" },
        { "value": "pint", "label": "Laravel Pint" },
        { "value": "ruff", "label": "Ruff" },
        { "value": "gofmt", "label": "gofmt" },
        { "value": "rustfmt", "label": "rustfmt" },
        { "value": "clang-format", "label": "clang-format" },
        { "value": "mix", "label": "mix format" },
        { "value": "terraform", "label": "terraform fmt" },
        { "value": "shfmt", "label": "shfmt" }
      ],
      "hint": "Formatters that must never run, even when the project is set up for them."
    },
    {
      "key": "sec_limits",
      "label": "Limits",
      "type": "section",
      "hint": "Timeout and file-size guards for the formatter subprocess."
    },
    {
      "key": "timeoutMs",
      "label": "Formatter timeout (ms)",
      "type": "number",
      "min": 5000,
      "max": 60000,
      "step": 1000,
      "placeholder": "10000",
      "default": 10000,
      "hint": "How long a formatter subprocess may run before it's killed. Effective ceiling is ~12s: the formatter runs inside the tools.call.after hook's budget, so a timeout at or above ~12s gets cut off by that budget before it can fire."
    },
    {
      "key": "maxFileBytes",
      "label": "Max file size to format (bytes)",
      "type": "number",
      "min": 262144,
      "max": 10485760,
      "step": 262144,
      "placeholder": "1048576",
      "default": 1048576,
      "hint": "Files larger than this are never formatted (skipped)."
    }
  ]
}
