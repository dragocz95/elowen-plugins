{
  "name": "onedrive",
  "version": "0.3.1",
  "apiVersion": "2",
  "requiresCore": "0.28.50",
  "description": "Syncs each project both ways with each person's OneDrive. Files created by the agent appear there without being shared in chat.",
  "entry": "dist/index.js",
  "userGrantable": true,
  "consumesControls": [
    "sandbox",
    "microsoftIdentity"
  ],
  "provides": {
    "apiRoutes": [
      "overview",
      "folders",
      "connect",
      "disconnect",
      "pause",
      "sync-now",
      "conflicts",
      "conflicts/resolve"
    ]
  },
  "capabilities": {
    "reads": [
      "db",
      "stores",
      "controls",
      "project-files"
    ],
    "network": true
  },
  "configSchema": [
    {
      "key": "rootFolder",
      "label": "OneDrive folder",
      "type": "string",
      "default": "Elowen",
      "hint": "Top-level folder created in each person's own OneDrive. Projects and sandbox workspaces are mirrored into separate subfolders of it."
    },
    {
      "key": "intervalSeconds",
      "label": "Sync interval",
      "type": "number",
      "default": 30,
      "hint": "How often a connected mirror is checked, in seconds. Lower values react faster and cost more Microsoft Graph calls."
    },
    {
      "key": "maxFileMb",
      "label": "Largest file",
      "type": "number",
      "default": 100,
      "hint": "Files above this size are skipped in both directions and reported, rather than silently held back."
    },
    {
      "key": "extraIgnore",
      "label": "Additional ignored paths",
      "type": "tokenList",
      "default": [],
      "hint": "Glob patterns excluded on top of the project's own .gitignore. Version control internals, dependencies and credentials are always excluded and cannot be re-enabled here."
    },
    {
      "key": "applyRemoteDeletions",
      "label": "Apply deletions from OneDrive",
      "type": "boolean",
      "default": true,
      "hint": "When someone deletes a mirrored file in OneDrive, its local copy moves to the sync trash. The file is never deleted permanently and can be restored at any time."
    }
  ],
  "web": {
    "entry": "web/index.js",
    "css": "web/index.css",
    "requiresApiVersion": 8,
    "project": [
      {
        "id": "mirror",
        "label": "OneDrive",
        "icon": "Cloud"
      }
    ],
    "strings": {
      "title": "OneDrive",
      "status": "Status",
      "connectCta": "Connect OneDrive",
      "connectHint": "Sync this project both ways with your OneDrive.",
      "connectConfirm": "Start mirroring",
      "notLinked": "Your account is not connected to Microsoft.",
      "statusIdle": "In sync",
      "statusSyncing": "Syncing",
      "statusPaused": "Paused",
      "statusBlocked": "Waiting for confirmation",
      "confirmDeletions": "Remove {count} files from OneDrive",
      "statusError": "Needs attention",
      "lastSync": "Last sync",
      "never": "Never",
      "files": "Files",
      "folder": "Folder",
      "openFolder": "Open in OneDrive",
      "workspaces": "Sandbox workspaces",
      "workspacesHint": "Each workspace is mirrored into its own folder, separate from the project itself.",
      "conflicts": "Conflicts",
      "conflictsHint": "Both copies were kept. Choose which one takes precedence; neither is deleted.",
      "close": "Close",
      "keepLocal": "Use the project version",
      "keepRemote": "Use the OneDrive version",
      "syncNow": "Sync now",
      "pause": "Pause",
      "resume": "Resume",
      "disconnect": "Disconnect",
      "disconnectHint": "Stops mirroring. Files already in OneDrive stay where they are.",
      "mirrorScope": "What is mirrored",
      "mirrorScopeHint": "Files excluded by the project's .gitignore are not synced. .git, node_modules, .env, private keys and credential files are never synced. You can exclude more paths in the plugin settings. These protections cannot be turned off.",
      "chooseFolder": "Folder to sync",
      "chooseFolderHint": "The folder you choose is the boundary: its files and subfolders are mirrored in both directions, continuously. The rest of the project is not.",
      "mirrorWholeProject": "Mirror the whole project",
      "mirrorThisFolder": "Mirror this folder",
      "noSubfolders": "This folder has no subfolders that can be mirrored.",
      "openFolderLabel": "Open folder",
      "mirroredFolder": "Folder",
      "statusConflict": "Waiting for your decision",
      "statusFirstRun": "Waiting for the first sync",
      "blockedTitle": "Should OneDrive remove {count} files?",
      "blockedBody": "{count} mirrored files disappeared from the project at once, so nothing has been removed from OneDrive yet. If that was intentional, confirm this one sync. Otherwise check that the project folder is complete and sync again.",
      "destination": "OneDrive folder",
      "account": "OneDrive account",
      "selected": "Selected",
      "wholeProject": "Whole project",
      "conflictsEmpty": "No unresolved conflicts",
      "safetyHint": "Nothing is deleted permanently. A file removed in OneDrive is kept in the project under .elowen-trash, and when both sides change the same file, both versions are kept."
    }
  },
  "requiresControls": [
    "microsoftIdentity"
  ]
}
