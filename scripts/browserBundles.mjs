/** Sources and committed output directories for browser-only plugin bundles. */
export const BROWSER_BUNDLES = {
  pluginUi: { source: 'web-src', output: 'web' },
  chatbotEmbed: { source: 'embed-src', output: 'embed' },
};
